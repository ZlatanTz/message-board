<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Document</title>
</head>
<body>
   <ul>
      <% messages.forEach((msg) => { %>
          <li><%= msg.user %>: <%= msg.text %> (<%= msg.added %>) <button type="submit">Details</button></li>
      <% })  %>
  </ul>
</body>
</html>